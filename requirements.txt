# Core dependencies for preprocessing and training
# Using stable versions with pre-built wheels to avoid compilation issues

# Data processing - using versions with pre-built wheels
h5py==3.8.0
pandas==1.5.3
numpy==1.24.3
opencv-python==4.7.0.72
Pillow==9.5.0
tqdm==4.65.0

# Scientific computing
scipy==1.10.1
scikit-learn==1.2.2
scikit-image==0.20.0

# Visualization
matplotlib==3.7.1

# NLP/Tokenization (for CLIP)
ftfy==6.1.1
regex==2023.3.23

# Optional but useful
joblib==1.2.0

# NOTE: Install PyTorch separately with CUDA support:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
